var numeros = [1, 2, 3, 4, 5, 6, 7];

var min = numeros[0]

for (i = 0; i < numeros.length; i++) {
    if (min < numeros [i++] ) {

       (min = numeros [i++] )
    } else if (min > numeros [i++] ) {
        (numeros [i++] = min)
    } else {
        console.log (min);
    }
}
